version: '3.8'

services:
  whitelist-web:
    build: .
    container_name: whitelist-web
    ports:
      - "3000:3000"
    volumes:
      - whitelist-data:/app/data
    environment:
      - NODE_ENV=production
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-admin123}
      - SESSION_SECRET=${SESSION_SECRET:-}
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "wget", "-q", "--spider", "http://localhost:3000/api/auth/check" ]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  whitelist-data:
