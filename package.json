{
  "name": "openpath-monorepo",
  "version": "1.0.0",
  "description": "DNS-based URL whitelist enforcement system for educational environments",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "husky || true",
    "lint": "eslint . --max-warnings=0",
    "lint:fix": "eslint . --fix",
    "lint:shell": "shellcheck linux/lib/*.sh linux/scripts/**/*.sh",
    "clean": "npm run clean --workspaces --if-present && rm -rf node_modules",
    "test": "npm run test:shell && npm run test:api && npm run test:dashboard && npm run test:spa",
    "test:shell": "cd tests && bats *.bats",
    "test:api": "npm test --workspace=@openpath/api",
    "test:dashboard": "npm test --workspace=dashboard",
    "test:spa": "npm test --workspace=openpath-spa",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "verify": "npm run typecheck && npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "eslint": "^9.17.0",
    "@eslint/js": "^9.17.0",
    "typescript-eslint": "^8.20.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10"
  },
  "lint-staged": {
    "*.{js,ts,tsx,mjs,cjs}": [
      "eslint --fix"
    ]
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/balejosg/openpath.git"
  },
  "keywords": [
    "dns",
    "whitelist",
    "dnsmasq",
    "education",
    "content-filter"
  ],
  "license": "AGPL-3.0-or-later",
  "workspaces": [
    "api",
    "spa",
    "shared",
    "dashboard",
    "auth-worker",
    "firefox-extension"
  ],
  "overrides": {
    "esbuild": "^0.25.0",
    "qs": "^6.14.1"
  }
}