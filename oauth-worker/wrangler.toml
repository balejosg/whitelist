name = "whitelist-oauth"
main = "worker.js"
compatibility_date = "2024-01-01"

# Variables de entorno (secretos se configuran con wrangler secret put)
[vars]
FRONTEND_URL = "https://balejosg.github.io/whitelist"

# IMPORTANTE: Después de desplegar, configurar estos secretos:
# wrangler secret put GITHUB_CLIENT_ID
# wrangler secret put GITHUB_CLIENT_SECRET
# wrangler secret put WORKER_URL
